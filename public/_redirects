# Netlify-style redirects (harmless on other hosts).
# Legacy routes -> the canonical trajectory route. No catch-all: an unknown
# URL must 404 rather than render the homepage.
/trajectory.html   /trajectory   301
/business          /trajectory   301
/business/*        /trajectory   301
